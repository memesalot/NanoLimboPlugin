package ua.nanit.limbo;

// ponytail: hand-written instead of upstream's buildconfig Gradle plugin; bump with the project version
public final class BuildConfig {

    public static final String LIMBO_VERSION = "2.0.0";

    private BuildConfig() {
    }
}
